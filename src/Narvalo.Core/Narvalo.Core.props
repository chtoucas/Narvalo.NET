<?xml version="1.0" encoding="utf-8" ?>
<Project ToolsVersion="12.0"
         InitialTargets="_InitializeNarvaloCoreProps"
         xmlns="http://schemas.microsoft.com/developer/msbuild/2003">
  <PropertyGroup>
    <DefineConstants>$(DefineConstants);NO_INTERNALS_VISIBLE_TO</DefineConstants>
  </PropertyGroup>

  <Target Name="_InitializeNarvaloCoreProps">
    <Warning Text="We temporarely force internals to be invisible for $(AssemblyName)." />
  </Target>
</Project>